import 'package:flutter/material.dart';
class BuyerProfileScreenContent extends StatelessWidget {
  const BuyerProfileScreenContent({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('นี่คือหน้าโปรไฟล์ผู้ซื้อ'));
  }
}