import 'package:flutter/material.dart';
class SellerOrdersScreenContent extends StatelessWidget {
  const SellerOrdersScreenContent({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('นี่คือหน้าสำหรับดูรายการออเดอร์ของผู้ขาย'));
  }
}